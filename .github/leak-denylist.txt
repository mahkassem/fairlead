# One case-insensitive extended regex per line. Fairlead's code, docs and
# fixtures must never name a private project, company domain or data store.
# The private names themselves come from the LEAK_DENYLIST repository
# variable, since listing them here would publish them.
