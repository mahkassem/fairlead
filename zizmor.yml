# release.yml is generated by dist and must not be hand-edited; these are
# its known findings, ignored for the whole file since regeneration moves lines.
rules:
  excessive-permissions:
    ignore: [release.yml]
  template-injection:
    ignore: [release.yml]
  secrets-inherit:
    ignore: [release.yml]
  unpinned-images:
    ignore: [release.yml]
